import React from 'react';
import { NumberMachine } from 'src/components/NumberMachine/NumberMachine';

export const Features: React.FC = () => {
    return (
        <div>
            <h1>Features</h1>
            <NumberMachine />
        </div>
    );
};
